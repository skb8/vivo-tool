# Список приложений, которые LSPosed предложит выбрать в области действия модуля.
#
# Одна строка — одно имя пакета. Всё после '#' считается комментарием.
# Из этого файла при сборке генерируются:
#   - res/values/module_scope.xml  -> @array/module_scope (метаданные xposedscope)
#   - META-INF/xposed/scope.list   -> новый формат модулей LSPosed
#   - ModuleScope.packages         -> проверка scope в UI приложения
#
# Добавляя хук для нового приложения, обязательно добавьте его пакет сюда,
# иначе LSPosed не предложит это приложение в списке выбора.
#
com.android.camera               # доступные фичи камеры
com.android.settings             # картинка карточки «О телефоне»

# Другие частые цели (раскомментируйте, когда появятся хуки):
# android                        # системный фреймворк (system_server)
# com.android.systemui           # SystemUI / шторка, статусбар
# com.vivo.upslide               # жесты и шторка Vivo/Funtouch
# com.bbk.launcher2              # лончер Vivo
